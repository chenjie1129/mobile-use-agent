volcengine-python-sdk>=5.0.0
python-dotenv>=1.0.0

# 可选: 图片 EXIF GPS 元数据解析 (不装则跳过该来源)
Pillow>=9.0

# 可选: macOS 系统级精确定位 (不装则自动降级为 IP 城市级定位)
pyobjc-framework-CoreLocation>=10.0; sys_platform == "darwin"
