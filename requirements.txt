# 官方全家桶 SDK: install.sh 默认只从中精简安装 volcenginesdkcore (81 个文件),
# 完整安装约 5-10 分钟, 精简安装约 1 分钟, 两者运行行为完全一致
volcengine-python-sdk>=5.0.0

# macOS 系统 Python 3.9 链接 LibreSSL，urllib3 2.x 不支持该 TLS 实现
urllib3<2; python_version < "3.10" and sys_platform == "darwin"

# 图片 EXIF GPS 元数据解析 (定位来源之一, 不装则自动跳过该来源)
Pillow>=9.0
